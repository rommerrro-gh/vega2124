server {
  listen 8081;
  listen [::]:8081;
  default_type application/json;
  location = /health { return 200 '{"status":"UP","mode":"contract"}'; }
  location / { return 501 '{"code":"CONTRACT_ONLY","message":"Use a verified connector for live data"}'; }
}
